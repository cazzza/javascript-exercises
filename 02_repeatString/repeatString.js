const repeatString = function(string, num) {
    if (num < 0) {
        return 'ERROR'
} else
 //   randRepeat = Math.floor(Math.random() * number);
    return string.repeat(num)
};



// Do not edit below this line
module.exports = repeatString;
